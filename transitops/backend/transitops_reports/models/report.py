from odoo import models


class TransitopsReport(models.Model):
    _name = 'transitops.report'
    _description = 'Fleet KPI Placeholder'

    # TODO: Add KPI fields (e.g. total_cost, avg_consumption, fleet_utilisation)
